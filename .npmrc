# node:sqlite is not a dependency that can be polyfilled — on an older Node the
# app fails at runtime with an opaque module error. Fail at install instead,
# where the message names the actual problem.
engine-strict=true
